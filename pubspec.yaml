name: gg_dart_js_bridge_template
description: >
  Template showing how to expose a Dart package as a JavaScript and WebAssembly
  module with hand-typed TypeScript declarations.
version: 0.1.0
repository: https://github.com/ggsuite/gg_dart_js_bridge_template.git
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  web: ^1.1.1

dev_dependencies:
  gg_console_colors: ^2.1.5
  lints: ^6.1.0
  test: ^1.31.1
